{
  "name": "todo-backend",
  "version": "1.0.0",
  "description": "Simple Todo App Backend API",
  "type": "module",
  "main": "index.js",
  "private": true,

  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.0.0"
  },

  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"No tests configured\" && exit 1"
  },

  "keywords": ["nodejs", "express", "mongodb", "todo", "api", "rest"],
  "license": "MIT",

  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "mongoose": "^9.0.1",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
